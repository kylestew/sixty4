#![allow(unused)]

// Embedded-oriented stub main. Replace with MCU-specific init when integrating.
fn main() {
    // For embedded targets, `#[no_std]` and a different runtime may be used.
    // Keep this as a placeholder to avoid pulling desktop dependencies into the bin.
}
